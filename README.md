# redis-cache-in-.net-core
Redis is the industry-standard choice for distributed caching in .NET Core, especially for applications running behind a load balancer or in a microservices architecture. Unlike IMemoryCache, which is local to a single server instance, Redis ensures data consistency across all application instances and persists even after server restarts.
